namespace Mappy.Models
{
    public enum ActiveTab
    {
        Sections,
        Features,
        Starts,
        Attirbutes
    }

    public enum BandboxMode
    {
        Tile,
        Feature,
    }

    public enum FeaturePlacementMode
    {
        Selection,
        Line,
        Fill,
        Sporadic
    }
}
